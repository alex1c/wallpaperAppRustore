import { PreciseCalculatorScreen } from '@/features/wallpaper/precise/precise-calculator-screen'

/** Precise Mode route — per-wall geometry with doors and windows. */
export default function PreciseRoute() {
  return <PreciseCalculatorScreen />
}
